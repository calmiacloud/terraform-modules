name: AmiComponentAnsible
schemaVersion: "1.0"

phases:
  - name: build
    steps:
      - name: Install
        action: ExecuteBash
        inputs:
          commands:
            - sudo yum install -y ansible